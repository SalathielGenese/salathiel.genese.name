import { MetaPlugin } from '@/app/shared/plugins/meta';
import MetaOptions = MetaPlugin.MetaOptions;

export const metaOptions: MetaOptions = {
    titleTemplate: {
        empty: 'Salathiel Genese',
        filled: 'Salathiel Genese - {}',
    },
};
